---
'@hz-9/eslint-config-airbnb': patch
---

- test: add Vitest spec covering profile, rule modules and prettier-rules entry
- build: add `vitest.config.ts`, `tsconfig.json` (extends shared `packages/tsconfig.base.json`) and register `test` target in `project.json`
- docs: correct `last-commit` badge link in guide README to point to `hz-9/lint-nx`
