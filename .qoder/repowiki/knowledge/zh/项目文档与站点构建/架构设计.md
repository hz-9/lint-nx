- 采用 VuePress 2.0 (RC) 作为静态站点生成器，使用 Vite 作为底层打包工具。
- 内部结构分为配置层 (.vuepress/src)、内容层 (overview, guide, changelog) 和主题层 (vuepress-theme-hope)。
- 支持中英双语本地化，通过 locales 配置实现路径前缀区分 (/ 与 /zh-CN/)。
- 依赖管理独立于主仓库，在 .vuepress 目录下维护独立的 package.json 与 node_modules。