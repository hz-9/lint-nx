- 采用源码(src)到构建产物(lib)的分离架构，通过 esbuild 脚本将 CommonJS 源码编译为 ESM (.mjs) 和 CJS (.js) 双格式。
- 内部规则按语义拆分为多个子模块（如 best-practices, style, imports），并通过 profile 目录下的入口文件组合成不同预设（airbnb-ts, airbnb-prettier）。
- 同时兼容 ESLint 传统的 extends 继承模式与最新的 Flat Config (eslint.config.js) 数组模式，通过 plugins.js 共享插件实例。